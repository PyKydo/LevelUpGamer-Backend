-- Categorías de productos
-- (No se requiere inserción directa, ya que CategoriaProducto es un enum)

-- Roles de usuario
-- (No se requiere inserción directa, ya que RolUsuario es un enum)

-- Script de inicialización intencionalmente vacío
-- Usamos enums para categorías y roles; no se requiere seed.
-- No-op válido para H2 para evitar error de script vacío
SELECT 1;
